# Runtime caches and local state — regenerated by the module on use
# (both the PowerShell `data/` tree and the bash port's `bash/data/` tree)
data/.pricing-cache.json
data/.usage-log.jsonl
data/.last-load
bash/data/.pricing-cache.json
bash/data/.usage-log.jsonl
bash/data/.last-load
bash/data/.usage.db

# Logs
*.log

# OS detritus
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE local files
.vscode/
.idea/
*.swp
*~

# Local AI tooling state (oh-my-claudecode, codegraph, understand-anything)
.omc/
.codegraph/
.understand-anything/

# PowerShell module build artifacts
*.nupkg

# Local environment overrides
.env
.env.local
